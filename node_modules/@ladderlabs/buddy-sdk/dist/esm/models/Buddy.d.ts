/// <reference types="bn.js" />
/// <reference types="node" />
import { PublicKey } from "@solana/web3.js";
import { BuddyType, ReferrerType } from "../utils/type";
import * as anchor from "@project-serum/anchor";
import { Program } from "@project-serum/anchor";
import { Buddylink } from "../idl/buddylink";
export interface BuddyAccount {
    pda: PublicKey;
    authority: PublicKey;
    bump: number;
    buddyType: BuddyType;
    dateCreated: anchor.BN;
    isFrozen: boolean;
    referrerTreasuryPda: PublicKey;
    referrerType: ReferrerType;
    name: string;
    paddingName: Buffer;
}
export declare class Buddy {
    private program;
    account: BuddyAccount;
    wallet?: PublicKey;
    private usdcMint?;
    constructor(buddy: BuddyAccount, program: Program<Buddylink>, wallet?: PublicKey);
    refresh(): Promise<this>;
    buyGlobalBuddy(buddyName: string): Promise<anchor.web3.TransactionInstruction[]>;
    inject(usdcMint: PublicKey): this;
}
