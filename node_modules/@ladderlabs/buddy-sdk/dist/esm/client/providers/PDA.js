import { getBuddyPDA, getMemberPDA, getTreasuryPDA } from "../../utils/pda";
var PDA = /** @class */ (function () {
    function PDA(program) {
        this.program = program;
    }
    PDA.prototype.getTreasuryPDA = function (owners, shares, mint) {
        return getTreasuryPDA(this.program, owners, shares, mint);
    };
    PDA.prototype.getMemberPDA = function (organizationName, name) {
        return getMemberPDA(this.program, organizationName, name);
    };
    PDA.prototype.getProfilePDA = function (name) {
        return getBuddyPDA(this.program, name);
    };
    return PDA;
}());
export { PDA };
//# sourceMappingURL=PDA.js.map