import { PublicKey } from "@solana/web3.js";
export function parsePublicKey(mint) {
    if (mint && mint.toString() !== PublicKey["default"].toString()) {
        return mint;
    }
    return null;
}
//# sourceMappingURL=parse.js.map