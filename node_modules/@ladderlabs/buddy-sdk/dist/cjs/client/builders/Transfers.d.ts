import { Program } from "@project-serum/anchor";
import { PublicKey, TransactionInstruction } from "@solana/web3.js";
import { Buddylink } from "../../idl/buddylink";
export declare class Transfer {
    private program;
    private wallet?;
    constructor(program: Program<Buddylink>, wallet?: PublicKey);
    getProgramId(): PublicKey;
    transferRewards(memberPDA: PublicKey, amount: number, mint?: PublicKey, allowMultiLevel?: boolean, allowStats?: boolean): Promise<TransactionInstruction[]>;
    transferRewardsNoMultiLevel(receiveMemberPDA: PublicKey, amount: number, mint?: PublicKey, allowStats?: boolean): Promise<TransactionInstruction[]>;
    transferRewardsWithBPS(memberPDA: PublicKey, amount: number, mint?: PublicKey, allowMultiLevel?: boolean, allowStats?: boolean): Promise<TransactionInstruction[]>;
    transferRewardsGlobalBuddies(buddyPDA: PublicKey, amount: number, mint?: PublicKey): Promise<TransactionInstruction[]>;
    transferRewardsStarAtlas(amount: number, volume: number, memberPDA: PublicKey, mint: PublicKey, allowStats?: boolean, programSigner?: PublicKey): Promise<TransactionInstruction[]>;
}
