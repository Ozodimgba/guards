import { Program } from "@project-serum/anchor";
import { PublicKey } from "@solana/web3.js";
import { Buddylink } from "../../idl/buddylink";
import { Buddy } from "../../models/Buddy";
export declare class BuddyProvider {
    private program;
    private wallet?;
    constructor(program: Program<Buddylink>, wallet?: PublicKey);
    isBuddyAvailable(name: string): Promise<boolean>;
    getAllBuddies(): Promise<Buddy[]>;
    getByPDA(buddyPDA: PublicKey): Promise<Buddy | null>;
    getByName(name: string): Promise<Buddy | null>;
    getAllByProfile(profilePDA: PublicKey): Promise<Buddy[]>;
    getProfile(wallet?: PublicKey): Promise<Buddy | null>;
}
