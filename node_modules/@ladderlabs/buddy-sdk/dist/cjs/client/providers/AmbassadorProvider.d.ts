import { Program } from "@project-serum/anchor";
import { PublicKey } from "@solana/web3.js";
import { Buddylink } from "../../idl/buddylink";
import { Ambassador } from "../../models/Ambassador";
import { Organization } from "../../models/Organization";
export declare class AmbassadorProvider {
    private program;
    private wallet?;
    constructor(program: Program<Buddylink>, wallet?: PublicKey);
    getByOrganization(organization: Organization): Promise<Ambassador | null>;
    getByPDA(ambassadorPDA: PublicKey): Promise<Ambassador | null>;
    getByName(name: string): Promise<Ambassador | null>;
}
