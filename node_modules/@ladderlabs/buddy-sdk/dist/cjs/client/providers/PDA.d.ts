import { Program } from "@project-serum/anchor";
import { Buddylink } from "../../idl/buddylink";
import { PublicKey } from "@solana/web3.js";
export declare class PDA {
    private program;
    constructor(program: Program<Buddylink>);
    getTreasuryPDA(owners: PublicKey[], shares: number[], mint?: PublicKey): PublicKey;
    getMemberPDA(organizationName: string, name: string): PublicKey;
    getProfilePDA(name: string): PublicKey;
}
