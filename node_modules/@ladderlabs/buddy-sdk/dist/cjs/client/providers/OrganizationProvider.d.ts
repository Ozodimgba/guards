import { Program } from "@project-serum/anchor";
import { PublicKey } from "@solana/web3.js";
import { Buddylink } from "../../idl/buddylink";
import { Organization } from "../../models/Organization";
export declare class OrganizationProvider {
    private program;
    private wallet?;
    constructor(program: Program<Buddylink>, wallet?: PublicKey);
    getByPDA(organizationPDA: PublicKey): Promise<Organization | null>;
    getByName(name: string): Promise<Organization | null>;
    getByWallet(wallet?: PublicKey): Promise<Organization[]>;
}
