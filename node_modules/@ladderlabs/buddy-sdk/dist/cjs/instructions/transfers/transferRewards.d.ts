import { Program } from "@project-serum/anchor";
import { Buddylink } from "../../idl/buddylink";
import { PublicKey, TransactionInstruction } from "@solana/web3.js";
export declare function transferRewards(program: Program<Buddylink>, authority: PublicKey, memberPDA: PublicKey, amount: number, //With lamport decimals
allowMultiLevel: boolean, allowStats: boolean, mint: PublicKey): Promise<TransactionInstruction[]>;
