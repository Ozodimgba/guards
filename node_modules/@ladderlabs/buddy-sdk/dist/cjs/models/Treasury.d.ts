/// <reference types="bn.js" />
import { PublicKey } from "@solana/web3.js";
import * as anchor from "@project-serum/anchor";
import { TreasuryOwner } from "../utils/type";
import { Buddylink } from "../idl/buddylink";
import { Program } from "@project-serum/anchor";
import { Buddy } from "./Buddy";
export interface TreasuryAccount {
    pda: PublicKey;
    bump: number;
    amountNoMultiLevel: anchor.BN;
    totalBps: number;
    mint: PublicKey;
    owners: TreasuryOwner[];
}
export declare class Treasury {
    private program;
    account: TreasuryAccount;
    wallet?: PublicKey;
    constructor(treasury: TreasuryAccount, program: Program<Buddylink>, wallet?: PublicKey);
    refresh(): Promise<this>;
    getOwners(): Promise<Buddy[]>;
    getClaimableBalance(buddyPDA?: PublicKey): Promise<number>;
    claim(buddyPDA?: PublicKey, isMultiLevel?: boolean): Promise<anchor.web3.TransactionInstruction[]>;
    transferUp(buddyPDA: PublicKey, treasuryPDA: PublicKey): Promise<anchor.web3.TransactionInstruction>;
}
