/// <reference types="node" />
import { PublicKey } from "@solana/web3.js";
import { Program } from "@project-serum/anchor";
import { Buddylink } from "../idl/buddylink";
export interface AmbassadorAccount {
    pda: PublicKey;
    authority: PublicKey;
    bump: number;
    name: string;
    padding: Buffer;
}
export declare class Ambassador {
    private program;
    account: AmbassadorAccount;
    wallet?: PublicKey;
    constructor(ambassador: AmbassadorAccount, program: Program<Buddylink>, wallet?: PublicKey);
    refresh(): Promise<this>;
}
